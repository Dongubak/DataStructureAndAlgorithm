for (int i = 0; i < g.size - 1; i++) {
   //    for (auto& e : g.G[i]) {
   //       if (distance[i] != std::numeric_limits<int>::max() &&
   //           distance[i] + e.weight < distance[e.dst]) {
   //          printf("음수 사이클 존재!\n");
   //          return {};
   //       }
   //    }
   // }