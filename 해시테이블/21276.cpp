#include <iostream>
#include <set>

struct Order {
   bool operator() (std::set<string, pair>& m, const std::string& left, const std::string& right) {
      if(m[])
   }
}

int main(void) {

   return 0;
}