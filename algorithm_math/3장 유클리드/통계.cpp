#include <bits/stdc++.h>
using namespace std;

int main(void) {

   multimap<int, int> mm;
   


   // int n;
   // cin >> n;

   // vector<int> s(n);

   // int sum = 0;

   // for(int i = 0; i < n; i++) {
   //    cin >> s[i];
   //    sum += s[i];
   // }

   // cout << int(round(sum / double(n))) << '\n';
   // sort(s.begin(), s.end());
   // cout << s[n / 2] << '\n';

   // map<int, int> m;
   
   // for(int i = 0; i < n; i++) {
   //    m[s[i]]++;
   // }

   // int maxKey = m.begin()->first;
   // int maxValue = m.begin()->second;

   // for(auto ptr = m.begin(); ptr != m.end(); ptr++) {
   //    if(ptr->second > maxValue) {
   //       maxKey = ptr->first;
   //    }
   // }

   // cout << maxKey << '\n';

   // cout << s[s.size() - 1] - s[0];

   return 0;
}